#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0); 
    cin.tie(0); 
    cout.tie(0);

    string s;
    cin >> s;

    int dp[5010]{ 0 };
    dp[0] = 1;
    for(int i = 1; i < s.size(); i++) {
        dp[i] +=-
    }
}