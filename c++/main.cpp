#include <bits/stdc++.h>

using namespace std;

const int N = 3000010;
int a[N];

int main() {
    ios_base :: sync_with_stdio(false); 
    cin.tie(NULL); 
    cout.tie(NULL);

    int t;
    cin >> t;
    while(t--) {
        int n;
        cin >> n;
        bool skill = false;
        for(int i = 1; i <= n; i++) {
            cin >> a[i];
        }
    }
}